plugins {
    id 'idea'
    id 'com.google.protobuf' version '0.9.5'
}

group = 'guru.qa'
version = '1.1.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation("io.grpc:grpc-protobuf:${project.ext.grpcVersion}") {
        exclude group: 'com.google.protobuf'
    }
    implementation("io.grpc:grpc-stub:${project.ext.grpcVersion}") {
        exclude group: 'com.google.protobuf'
    }
    implementation "com.google.protobuf:protobuf-java-util:${project.ext.protobufVersion}"
    compileOnly 'jakarta.annotation:jakarta.annotation-api:1.3.5' // Java 9+ compatibility - Do NOT update to 2.0.0
}

protobuf {
    protoc {
        path = 'C:/tools/protobuf/bin/protoc.exe'
    }
    clean {
        delete generatedFilesBaseDir
    }
    plugins {
        grpc {
            path = 'C:/tools/protobuf/bin/protoc-gen-grpc-java.exe'
        }
    }
    generateProtoTasks {
        all()*.plugins {
            grpc {}
        }
    }
}
